class_name LevelUnlocks
extends RefCounted

## Single source of truth for what each Klippy level unlocks, so catalogs and
## menus reference a name here instead of a bare number.

const MARBLE_COSMETICS := 3
